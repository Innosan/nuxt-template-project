import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "com.innosan.nuxt.template",
	appName: "nuxt-template-project",
	webDir: ".output/public",
};

export default config;
